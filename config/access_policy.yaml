access_policy:
  allowed_tables:
    - "regions"
    - "categories"
    - "products"
    - "sales_reps"
    - "sales_rep_performance"
    - "sales_targets"
    - "monthly_sales"
    - "orders"
    - "order_items"
    - "customer_health"
    - "agent_query_log"
    - "suggested_questions"
    - "correlation_registry"

  blocked_columns:
    - "customer_health.renewal_probability"
    - "agent_query_log.session_id"

  max_rows: 1000
